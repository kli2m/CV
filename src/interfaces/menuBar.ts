export interface event {
  title: string;
  dates: string;
  description: string;
}
export interface MenuBarType {
  ABOUT: string;
  SKILLS: string;
  EXPERINCE: event[];
  EDUCATION: event[];
  LANGUAGES: event[];
}
